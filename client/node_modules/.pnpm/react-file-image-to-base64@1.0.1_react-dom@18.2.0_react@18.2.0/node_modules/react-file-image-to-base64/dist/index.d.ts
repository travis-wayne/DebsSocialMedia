import React from "react";
interface componentProps {
    onCompleted: (result: object[]) => void;
    multiple?: boolean;
    CustomisedButton?: React.FC<{
        triggerInput: () => void;
    }>;
    preferredButtonText?: string;
}
declare const ReactImageFileToBase64: {
    ({ onCompleted, multiple, CustomisedButton, preferredButtonText, }: componentProps): JSX.Element;
    defaultProps: {
        multiple: boolean;
    };
};
export default ReactImageFileToBase64;
